name: Build Docker

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Instalar Docker CLI
        run: |
          apt-get update
          apt-get install -y docker.io

      - name: Validar Docker
        run: |
          docker version